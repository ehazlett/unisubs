language: python
python:
  - "2.5"
  - "2.6"
  - "2.7"
install:
  # Build/test dependencies
  - pip install -r deploy/requirements.txt --use-mirrors
  - pip install -r deploy/requirements-test.txt --use-mirrors
script: python manage.py test
notifications:
  irc:
    channels: "irc.freenode.org#vitasso"
    on_success: change
    on_failure: change
